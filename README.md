# JobCodex

Job copilot for discovery, matching, tailoring, and tracking.

## Setup

```bash
pip install -e ".[dev]"
```

## Run

```bash
uvicorn backend.main:app --reload
```

## Test

```bash
pytest tests/ -v
```
